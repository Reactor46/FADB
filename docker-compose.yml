services:
  # MARIADB-BEGIN
  # mariadb:
  #   image: mariadb:10.11
  #   container_name: fadb-mariadb
  #   restart: unless-stopped
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD}
  #     - MYSQL_DATABASE=${MARIADB_DATABASE}
  #     - MYSQL_USER=${MARIADB_USER}
  #     - MYSQL_PASSWORD=${MARIADB_PASSWORD}
  #   volumes:
  #     - ./mariadb_data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"
  # MARIADB-END

  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    image: reactor46/fadb-app:latest
    container_name: fadb-app
    restart: unless-stopped
    volumes:
      - ./data:/usr/src/app/data
    environment:
      - NODE_ENV=production
      - PORT=5000
      # - DB_TYPE=mariadb
      # - DB_HOST=mariadb
      # - DB_PORT=3306
      # - DB_NAME=${MARIADB_DATABASE}
      # - DB_USER=${MARIADB_USER}
      # - DB_PASS=${MARIADB_PASSWORD}
    ports:
      - "8081:5000"
    depends_on:
      # - mariadb
      - ""
    # TRAEFIK-BEGIN
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.fadb.rule=Host(`fadb.local`)"
    #   - "traefik.http.routers.fadb.tls=true"
    #   - "traefik.http.routers.fadb.tls.certresolver=letsencrypt"
    #   - "traefik.http.services.fadb.loadbalancer.server.port=5000"
    # TRAEFIK-END

networks:
  default:
    name: fadb_net
